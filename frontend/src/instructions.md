
1. Open Firefox
2. Type in about:debugging into the url
3. Go to `This Firefox` tab on the left
4. Click on `Load Temporary Add-on...`
5. Find the `manifest.json` file that is inside the `frontend` folder
6. When you make changes, click on `Reload` in the about:debugging tab, then refresh the canvas tab.